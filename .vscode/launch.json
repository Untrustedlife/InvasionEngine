{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open via localhost",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:5500/index.html",
      "webRoot": "${workspaceFolder}/dist",
      "preLaunchTask": "serve-python-http-5500"
    }
  ]
}
